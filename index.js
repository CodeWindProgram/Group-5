const express = require("express");
const app = express();
const pool = require("./db")

app.use(express.json()) // => req.body

// ROUTES //

// Login Data

// Register Data

// Answers Submission

// Results